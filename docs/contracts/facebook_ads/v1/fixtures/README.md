# Synthetic Contract Fixtures

Every JSON file is synthetic and safe for Product contract tests. The `schema` or `schemas` field names components in `../openapi.json`; `example` contains the fixture value. These files are not database exports and contain no customer or SQLite data.
